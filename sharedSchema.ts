/// The main schema for objects and inputs

import { graphql } from "./deps.ts";

const createSharedSchemaFiles = (schema: graphql.GraphQLSchema) => {
  // We want to make a file with everything except Query and Mutation in it.
};
