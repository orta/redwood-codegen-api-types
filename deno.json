{
  "fmt": {
    "options": {
      "lineWidth": 140
    }
  },
  "tasks": {
    "dev": "deno run --allow-all --unstable --watch dev.ts",
    "test": "deno test --allow-all",
    "build:npm": "deno run --allow-all scripts/buildNPM.ts",
    "deploy:npm": "cd npm && npm publish && cd .."
  }
}
